# Azure AI Speech 发音评估 JSON/Offset 操作指南

这个目录是一个独立 demo，目标不是只看总分，而是拿到 Azure 返回的原始 JSON：

- 单词级 `Words`
- 音节级 `Syllables`
- 音素级 `Phonemes`
- 每一层的 `Offset` / `Duration`
- 每个音素的 `AccuracyScore`
- 实际发音候选 `NBestPhonemes`

## 一、网页上需要做什么

### 1. 创建 Speech 资源

1. 打开 Azure Portal：<https://portal.azure.com/>
2. 搜索 `Speech services`，创建一个 Speech 资源。
3. 区域选择你已经给出的 `eastus`。
4. Pricing tier 初次测试可以选 Free tier，如果当前订阅可用。
5. 创建完成后进入这个资源。

### 2. 拿 Key 和 Region

1. 在资源左侧菜单打开 `Keys and Endpoint`。
2. 复制 `KEY 1` 或 `KEY 2`。
3. 确认 `Location/Region` 是 `eastus`。
4. 打开本目录的 `.env` 文件，只填：

```env
SPEECH_KEY=你的真实key
SPEECH_REGION=eastus
```

不要把 `.env` 上传到任何地方。

### 3. 可选：先在网页上体验

你也可以打开 Microsoft Foundry 的 Pronunciation Assessment 工具：

<https://ai.azure.com/explore/aiservices/speech/pronunciationassessment>

在网页里可以：

1. 选择 Reading。
2. 选择语言，比如 `English (United States)` / `en-US`。
3. 输入参考文本。
4. 录音或上传音频。
5. 看评估结果。
6. 切到 JSON 视图，观察 `Words`、`Syllables`、`Phonemes`。

网页适合先理解返回结构；真正批量处理或接入系统时，用本目录的代码。

## 二、代码上怎么跑

### 1. 准备音频

第一次测试请用短 WAV：

- 最好小于 30 秒
- WAV / PCM
- 单声道
- 16 kHz 或 48 kHz 都可以先试

参考文本要和你读的内容一致，比如音频里读的是：

```text
hello world
```

那命令里的 `ReferenceText` 就写 `hello world`。

### 2. PowerShell 一条命令运行

进入本目录：

```powershell
cd C:\Users\yeche\Desktop\CodexRelatedProject\RandomAsk\azure-pronunciation-assessment-json
```

运行：

```powershell
.\run_assessment.ps1 `
  -AudioFile "C:\path\to\your_audio.wav" `
  -ReferenceText "hello world" `
  -SpeechRegion eastus
```

这个脚本会：

1. 创建 `.venv`
2. 安装 Azure Speech SDK
3. 如果环境里没有 `SPEECH_KEY`，会提示你粘贴 key
4. 调用 `assess_pronunciation.py`
5. 在 `outputs` 目录输出 JSON 和 CSV

### 3. 直接运行 Python

如果你已经填好了 `.env`，也可以直接跑：

```powershell
.\.venv\Scripts\python.exe assess_pronunciation.py `
  --audio-file "C:\path\to\your_audio.wav" `
  --reference-text "hello world" `
  --language en-US `
  --phoneme-alphabet IPA `
  --nbest-phoneme-count 5
```

## 三、输出怎么看

运行成功后，会在 `outputs` 里看到：

- `*_raw.json`：Azure 原始完整 JSON。
- `*_phonemes.csv`：把每个 phoneme 展平成一行，方便看 offset。
- `*_syllables.csv`：把每个 syllable 展平成一行。

你最关心的 JSON 路径通常是：

```text
NBest[0].Words[*].Offset
NBest[0].Words[*].Duration
NBest[0].Words[*].Syllables[*].Offset
NBest[0].Words[*].Syllables[*].Duration
NBest[0].Words[*].Phonemes[*].Offset
NBest[0].Words[*].Phonemes[*].Duration
NBest[0].Words[*].Phonemes[*].PronunciationAssessment.AccuracyScore
NBest[0].Words[*].Phonemes[*].PronunciationAssessment.NBestPhonemes
```

`Offset` 和 `Duration` 的单位是 100 纳秒 tick。

换算成秒：

```text
seconds = ticks / 10000000
```

例如：

```text
Offset = 11700000
seconds = 1.17
```

表示这个音素大约从音频第 1.17 秒开始。

## 四、几个重要参数

代码里关键配置在 `assess_pronunciation.py`：

```python
speechsdk.PronunciationAssessmentConfig(
    reference_text=args.reference_text,
    grading_system=speechsdk.PronunciationAssessmentGradingSystem.HundredMark,
    granularity=speechsdk.PronunciationAssessmentGranularity.Phoneme,
    enable_miscue=args.enable_miscue,
)
```

重点是：

- `granularity=Phoneme`：必须这样才会拿到音素级结果。
- `phoneme_alphabet=IPA`：输出 IPA 音标，比如 `h ɛ l oʊ`。
- `nbest_phoneme_count=5`：每个预期音素返回多个实际发音候选。
- `enable_miscue`：用于参考文本对比，识别 omission/insertion。
- `enable_prosody`：可选，`en-US` 支持，可能有额外费用。

## 五、下一步

先用一段 2 到 5 秒的 WAV 跑通。跑通后，我们再做两件事：

1. 把 `raw.json` 里的结构按你的业务需要整理成你要的字段。
2. 如果音频超过 30 秒，改成 continuous recognition 模式。

